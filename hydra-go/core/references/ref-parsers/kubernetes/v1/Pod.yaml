ref-parsers:
  - gvk: v1/Pod
    pick:
      # Pod -> Secret (imagePullSecrets)
      - cel: "entity.spec.imagePullSecrets.map(ips, refBuilder().outgoing(id('v1/Secret', ns, ips.name)))"
        label: imagePullSecret
      # Pod -> ServiceAccount
      - cel: "[refBuilder().outgoing(id('v1/ServiceAccount', ns, entity.spec.serviceAccountName))]"
        label: serviceAccount
      # Pod -> ConfigMap (volumes)
      - cel: "entity.spec.volumes.filter(v, has(v.configMap)).map(v, (has(v.configMap.optional) && v.configMap.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, v.configMap.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, v.configMap.name))))"
        label: volume
      # Pod -> Secret (volumes)
      - cel: "entity.spec.volumes.filter(v, has(v.secret)).map(v, (has(v.secret.optional) && v.secret.optional ? refBuilder().outgoing(id('v1/Secret', ns, v.secret.secretName)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, v.secret.secretName))))"
        label: volume
      # Pod -> PersistentVolumeClaim (volumes)
      - cel: "entity.spec.volumes.filter(v, has(v.persistentVolumeClaim)).map(v, refBuilder().outgoing(id('v1/PersistentVolumeClaim', ns, v.persistentVolumeClaim.claimName)))"
        label: volume
      # Pod -> ConfigMap (envFrom)
      - cel: "entity.spec.containers.map(c, has(c.envFrom) ? c.envFrom.filter(e, has(e.configMapRef)).map(e, (has(e.configMapRef.optional) && e.configMapRef.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, e.configMapRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, e.configMapRef.name)))) : []).flatten()"
        label: envFrom
      # Pod -> Secret (envFrom)
      - cel: "entity.spec.containers.map(c, has(c.envFrom) ? c.envFrom.filter(e, has(e.secretRef)).map(e, (has(e.secretRef.optional) && e.secretRef.optional ? refBuilder().outgoing(id('v1/Secret', ns, e.secretRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, e.secretRef.name)))) : []).flatten()"
        label: envFrom
      # Pod -> ConfigMap (env valueFrom)
      - cel: "entity.spec.containers.map(c, has(c.env) ? c.env.filter(e, has(e.valueFrom) && has(e.valueFrom.configMapKeyRef)).map(e, (has(e.valueFrom.configMapKeyRef.optional) && e.valueFrom.configMapKeyRef.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, e.valueFrom.configMapKeyRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, e.valueFrom.configMapKeyRef.name)))) : []).flatten()"
        label: env
      # Pod -> Secret (env valueFrom)
      - cel: "entity.spec.containers.map(c, has(c.env) ? c.env.filter(e, has(e.valueFrom) && has(e.valueFrom.secretKeyRef)).map(e, (has(e.valueFrom.secretKeyRef.optional) && e.valueFrom.secretKeyRef.optional ? refBuilder().outgoing(id('v1/Secret', ns, e.valueFrom.secretKeyRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, e.valueFrom.secretKeyRef.name)))) : []).flatten()"
        label: env
      # Pod -> ConfigMap (projected volumes)
      - cel: "entity.spec.volumes.filter(v, has(v.projected)).map(v, v.projected.sources.filter(s, has(s.configMap)).map(s, (has(s.configMap.optional) && s.configMap.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, s.configMap.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, s.configMap.name))))).flatten()"
        label: volume
      # Pod -> Secret (projected volumes)
      - cel: "entity.spec.volumes.filter(v, has(v.projected)).map(v, v.projected.sources.filter(s, has(s.secret)).map(s, (has(s.secret.optional) && s.secret.optional ? refBuilder().outgoing(id('v1/Secret', ns, s.secret.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, s.secret.name))))).flatten()"
        label: volume
      # Pod -> ConfigMap (initContainers envFrom)
      - cel: "entity.spec.initContainers.map(c, has(c.envFrom) ? c.envFrom.filter(e, has(e.configMapRef)).map(e, (has(e.configMapRef.optional) && e.configMapRef.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, e.configMapRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, e.configMapRef.name)))) : []).flatten()"
        label: "initContainer envFrom"
      # Pod -> Secret (initContainers envFrom)
      - cel: "entity.spec.initContainers.map(c, has(c.envFrom) ? c.envFrom.filter(e, has(e.secretRef)).map(e, (has(e.secretRef.optional) && e.secretRef.optional ? refBuilder().outgoing(id('v1/Secret', ns, e.secretRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, e.secretRef.name)))) : []).flatten()"
        label: "initContainer envFrom"
      # Pod -> ConfigMap (initContainers env valueFrom)
      - cel: "entity.spec.initContainers.map(c, has(c.env) ? c.env.filter(e, has(e.valueFrom) && has(e.valueFrom.configMapKeyRef)).map(e, (has(e.valueFrom.configMapKeyRef.optional) && e.valueFrom.configMapKeyRef.optional ? refBuilder().outgoing(id('v1/ConfigMap', ns, e.valueFrom.configMapKeyRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/ConfigMap', ns, e.valueFrom.configMapKeyRef.name)))) : []).flatten()"
        label: "initContainer env"
      # Pod -> Secret (initContainers env valueFrom)
      - cel: "entity.spec.initContainers.map(c, has(c.env) ? c.env.filter(e, has(e.valueFrom) && has(e.valueFrom.secretKeyRef)).map(e, (has(e.valueFrom.secretKeyRef.optional) && e.valueFrom.secretKeyRef.optional ? refBuilder().outgoing(id('v1/Secret', ns, e.valueFrom.secretKeyRef.name)).tag('optional:ref') : refBuilder().outgoing(id('v1/Secret', ns, e.valueFrom.secretKeyRef.name)))) : []).flatten()"
        label: "initContainer env"
      # Pod -> Service (label selector matching)
      - cel: "matchingServices(ns, entity.labels()).map(s, refBuilder().incoming(ref('service', s)))"
