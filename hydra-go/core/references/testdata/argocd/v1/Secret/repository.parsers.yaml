ref-parsers:
  - predicate: 'gvk == "v1/Secret" && entity.metadata.labels["argocd.argoproj.io/secret-type"] == "repository"'
    pick:
      - cel: '[refBuilder().incoming(ref("argocd.argoproj.io/repository", string(base64.decode(entity.data.url))))]'
