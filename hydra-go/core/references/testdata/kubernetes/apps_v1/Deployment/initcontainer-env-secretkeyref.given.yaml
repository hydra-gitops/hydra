apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      initContainers:
        - name: init
          image: busybox
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: init-secret
                  key: password
      containers:
        - name: app
          image: nginx
---
apiVersion: v1
kind: Secret
metadata:
  name: init-secret
  namespace: default
type: Opaque
data:
  password: cGFzc3dvcmQ=
