# Builtin: k3s.cattle.io Addon kube-system/local-storage — activates local-path-provisioner.
id: k3s-addon-local-storage
defaultEnabled: false
activates:
  - local-path-provisioner
predicates:
  addon:
    ids:
      - 'k3s.cattle.io/v1/Addon/kube-system/local-storage'
