# Builtin: k3s.cattle.io Addon kube-system/metrics-apiservice — activates metrics-server.
id: k3s-addon-metrics-apiservice
defaultEnabled: false
activates:
  - metrics-server
predicates:
  addon:
    ids:
      - 'k3s.cattle.io/v1/Addon/kube-system/metrics-apiservice'
