# Builtin: k3s.cattle.io Addon kube-system/coredns — activates the coredns cluster-defaults preset.
id: k3s-addon-coredns
defaultEnabled: false
activates:
  - coredns
predicates:
  addon:
    ids:
      - 'k3s.cattle.io/v1/Addon/kube-system/coredns'
